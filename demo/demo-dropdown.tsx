import {Button, PageCard, PopConfirm, Message, Box, Notification, BalloonConfirm, Loading} from "../src/index";


function DemoDropdown() {

    return (
        <PageCard>
            <Box direction="row" spacing={20}>
                DemoDropdown
            </Box>
        </PageCard>
    );
}

export {
    DemoDropdown
}
