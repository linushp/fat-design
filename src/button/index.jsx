import ButtonGroup from './view/group';
import {Button1 as Button, SaveButton, ActionButton, LoadingButton } from './comps.jsx'

Button.Group = ButtonGroup;
Button.SaveButton = SaveButton;
Button.ActionButton = ActionButton;
Button.LoadingButton = LoadingButton;

export default Button;
