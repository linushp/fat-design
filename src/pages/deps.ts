import ResponsiveGrid from '../responsive-grid';
import Grid0 from '../grid';
import Balloon0 from '../balloon';
import Icon0 from '../icon'

const RGrid: any = ResponsiveGrid;
const Grid: any = Grid0;

const Balloon: any = Balloon0;
const Icon: any = Icon0;

export {
    Grid,
    RGrid,
    Icon,
    Balloon
}
